@model IEnumerable<LiperFrontend.Models.SubCategory>

@{
    ViewData["Title"] = "CategorySubCategories";
}

<h1>CategorySubCategories</h1>

<p>
   
    @Html.ActionLink("New Sub Category","Create", new { id=Context.GetRouteValue("Id")} ,  new { @class="btn btn-outline-success"}) 

   @*  @if (Model.Count() > 0)
    {
        <a asp-action="Create" asp-route-categoryId="@Model.FirstOrDefault().categoryId" class="btn btn-outline-success">Create New</a>

    }
    else
    {
        <a asp-action="Create" asp-route-categoryId="1" class="btn btn-outline-success">Create New</a>

    } *@
    <a asp-action="Index" asp-controller="Categories" class="btn btn-outline-secondary">Back to Categories List</a>
    </p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nameEN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nameAR)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nameEN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nameAR)
            </td>
            <td>
                    <a asp-action="Edit"  class="btn btn-outline-info" asp-route-Id=@item.id> Edit</a>
                    <a asp-action="Delete" class="btn btn-outline-danger" asp-route-Id=@item.id>Delete</a>

            </td>
        </tr>
}
    </tbody>
</table>
